# workspace

Local working area for projects created by AI agents.

Rules:
- create each new project in `workspace/<project-name>/`
- do not place application code in the repository root
- keep project docs, artifacts, and temporary files inside that project directory
- project contents are git-ignored by default; only this README and `AGENTS.md` stay tracked
